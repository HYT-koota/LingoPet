# LingoPet 调试待办事项 - 2026年2月5日

## 项目状态
- **分支**: login 分支（子文件夹 `LingoPet\`，worktree）
- **数据存储**: Supabase 云存储（已配置 ✅）
- **主要问题**: 单词保存后界面持续转圈，复习功能不工作

## 问题诊断

### 核心问题
1. **单词保存挂起**：查询单词后显示结果但界面持续转圈，`saveWord` 函数被调用但未完成
2. **复习功能不工作**：点击复习按钮后只有 `[Review] Starting new review` 日志，无后续输出
3. **共同根源**：`supabase.auth.getUser()` 认证调用可能挂起

### 具体现象
- 查询单词 "render" 后控制台日志：
  ```
  [Dictionary] handleSearch called for: render
  [Dictionary] Attempting to save word: render ID: ...
  ```
  但**没有**后续的 `[saveWord]` 日志
- 点击 "Daily Review" 按钮只有 `[Review] Starting new review`，无后续日志
- 界面显示单词释义但持续加载状态（转圈）
- Supabase 配置显示已配置 ✅（URL 和 Anon Key）

## 已实施的修复

### 1. 调试日志增强
- **App.tsx**：添加详细渲染日志、复习函数调试日志、日期对比日志（同时显示本地和UTC日期）
- **Dictionary.tsx**：完整保存过程日志链、完整 WordEntry 对象 JSON 输出
- **supabaseDataService.ts**：`getWords` 和 `saveWord` 添加 `supabase.auth.getUser()` 完整结果日志

### 2. React Hooks 修复
- **App.tsx:21**：修复 React hooks 顺序错误，将 `console.log(mode)` 从函数顶部移至 `useEffect` 中

### 3. 超时保护机制
- **Dictionary.tsx**：为 `saveWord` 添加 10 秒超时保护
- **App.tsx**：为 `getWords` 添加 10 秒超时保护
- 防止认证调用无限期挂起

### 4. 日期时区处理
- **App.tsx**：同时检查本地日期（`toLocaleDateString('en-CA')`）和 UTC 日期（`toISOString()`）
- 解决单词保存时间与当前日期因时区不同而不匹配的问题

### 5. 错误处理增强
- **supabaseDataService.ts**：`saveWord` 函数用 `try-catch` 包装，记录所有错误
- **App.tsx**：`startReview` 函数添加 `try-catch`，捕获并显示错误

## 诊断发现

### 关键线索
1. **`saveWord` 开始执行但未完成**：看到 `[Dictionary] Attempting to save word` 但无 `[saveWord]` 日志
2. **认证可能挂起**：`supabase.auth.getUser()` 是 `saveWord` 和 `getWords` 的共同入口点
3. **无网络错误**：Supabase 配置正确，无 CORS 或网络错误报告

### 可能原因
1. **Supabase 认证会话无效/过期**：用户已登录但会话凭证无效
2. **网络请求被阻塞**：虽然配置正确，但实际请求可能被拦截
3. **异步操作死锁**：某个异步操作未正确处理 Promise

## 下一步测试计划

### 优先级 1：验证超时机制
1. 刷新页面，观察控制台初始日志（LingoPet 启动诊断、Supabase 配置）
2. 查询新单词（如 "test"），等待 10 秒观察是否出现超时错误
3. 点击 "Daily Review" 按钮，同样等待 10 秒观察超时

### 优先级 2：检查网络请求
1. 在浏览器开发者工具的 **Network** 标签中过滤 `supabase.co` 请求
2. 查询单词时观察是否有向 Supabase 发起的 `auth/user` 或 `words` 表请求
3. 检查请求状态码（200/401/403/500）

### 优先级 3：认证状态验证
1. 在控制台直接测试：`await supabase.auth.getUser()` 返回结果
2. 检查当前用户的登录状态
3. 尝试登出后重新登录

## 剩余问题列表

### 阻塞性问题
- [ ] `saveWord` 函数挂起，单词无法保存到 Supabase
- [ ] `getWords` 函数挂起，复习功能无法获取单词列表
- [ ] 界面持续转圈，用户体验中断

### 次要问题
- [ ] 宠物图片生成可能失败（但非当前核心）
- [ ] 登出按钮响应异常（部分响应但未完全登出）

## 建议的解决方向

### 短期方案（立即测试）
1. **验证超时机制**：确认 10 秒后是否出现超时错误，如果是则证明认证确实挂起
2. **检查网络请求**：查看实际向 Supabase 发起的请求状态
3. **手动测试认证**：在控制台直接调用 `supabase.auth.getUser()`

### 中长期方案
1. **添加认证状态检查**：在应用启动时验证用户认证状态
2. **实现认证重试机制**：当认证失败时提供重新登录选项
3. **优化错误处理**：更友好的错误提示和恢复选项

## 文件修改摘要

### 已修改文件
1. **`App.tsx`**：修复 hooks 顺序、添加复习函数超时保护、增强日期对比日志
2. **`Dictionary.tsx`**：添加保存超时保护、完整对象 JSON 日志
3. **`supabaseDataService.ts`**：增强认证调试日志、添加错误处理
4. **`PetNode.tsx`**：修复宠物占位图样式问题（`mix-blend-multiply` 不适用于 SVG）

### 待检查文件
1. **`supabaseClient.ts`**：确认 Supabase 客户端配置正确
2. **`.env.local`**：确认环境变量正确加载（VITE_SUPABASE_URL、VITE_SUPABASE_ANON_KEY）

## 注意事项

1. **分支架构**：当前工作在 login 分支（子文件夹），与 main 分支的 localStorage 架构完全不同
2. **数据持久性**：所有数据存储在 Supabase 云端，需确保网络连接和认证正常
3. **环境变量**：Vite 使用 `import.meta.env.VITE_*`，已修复 `vite.config.ts` 加载配置

---

**最后更新**：2026年2月4日
**下次工作重点**：验证超时机制，检查网络请求，解决认证挂起问题